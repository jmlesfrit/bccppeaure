{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ vente.Lieu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ vente.Date ? vente.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>N�lot</th>
                <td>{{ vente.N�lot }}</td>
            </tr>
            <tr>
                <th>IdLot</th>
                <td>{{ vente.IdLot }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vente_index') }}">back to list</a>

    <a href="{{ path('vente_edit', {'id': vente.id}) }}">edit</a>

    {{ include('vente/_delete_form.html.twig') }}
{% endblock %}
